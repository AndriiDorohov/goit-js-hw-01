const name = 'Protective Field Generator';

let price = 1000;
console.log(`Selected "${name}" price per unit is ${price} credits`);
price = 2000;
console.log(`Selected "${name}" price per unit is ${price} credits`);